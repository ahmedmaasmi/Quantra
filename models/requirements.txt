# Python ML Dependencies for Quantra Models
# Install with: pip install -r models/requirements.txt

# Core ML Libraries
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=2.3.0  # Python 3.14 compatible wheel available

# Gradient Boosting
xgboost>=2.0.0
lightgbm>=4.0.0

# Time Series Forecasting
prophet>=1.1.4
statsmodels>=0.14.0

# Deep Learning (Optional - only needed for KYC document validation)
# Note: TensorFlow may not be available for Python 3.14+ yet
# If you need KYC document validation, install manually: pip install tensorflow keras
# tensorflow>=2.13.0
# keras>=2.13.0

# Computer Vision & OCR
opencv-python>=4.8.0
pytesseract>=0.3.10
# face-recognition and dlib require CMake and Visual C++ build tools on Windows
# They are optional - KYC service gracefully handles their absence
# Install manually if needed: pip install dlib face-recognition
# face-recognition>=1.3.0
# dlib>=19.24.0

# NLP
transformers>=4.30.0
torch>=2.0.0

# Model Explainability (Optional - requires numba which doesn't support Python 3.14 yet)
# Note: These packages may not be available for Python 3.14+ yet
# If you need explainability features, install manually when compatible versions are available
# shap>=0.42.0
# lime>=0.2.0.1

# API Framework
flask>=2.3.0
flask-cors>=4.0.0
fastapi>=0.100.0
uvicorn>=0.23.0

# Utilities
joblib>=1.3.0
# pickle5 is not needed for Python 3.8+ (features are built-in)
python-dotenv>=1.0.0

# Cloud APIs (optional)
# google-cloud-vision>=3.4.0
# boto3>=1.28.0

